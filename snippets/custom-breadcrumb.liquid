<nav class="breadcrumb">
  <a href="/">Home</a>
  {%- if collection -%}
    › <a href="{{ collection.url }}">{{ collection.title }}</a>
  {%- endif -%}

  {%- if product -%}
    › <span>{{ product.title }}</span>
  {%- endif -%}
</nav>
